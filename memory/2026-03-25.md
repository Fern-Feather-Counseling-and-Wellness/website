Date: 2026-03-25

Summary:
- Nicole asked for direct, live access to the website and to be able to see edits in the browser.
- Frustration: repeated round-and-round about tokens, permissions, and the deploy flow. Wants a simple workflow: I make the change, save to GitHub, push -> Vercel auto-deploys, and the site is viewable.
- Preferences noted:
  - No screenshots as the only preview; wants real, navigable site in her browser.
  - Prefer quick, simple options when Vercel/GitHub are available: commit -> push -> deploy -> Drive backup.
  - Wants Ronda to remember these conversations and not repeat steps.

Actions taken today (record):
- Started local static server at 127.0.0.1:8000 to preview site locally.
- Created branch ff/preview-deploy, committed local edits, merged into master, and pushed master to origin.
- Attempted to check Vercel deploy via API but lacked provider token; domain (fernfeatherwellness.com) did not resolve from this environment.

Outstanding items / next steps (user choice required):
- Option A: Create temporary public tunnel (instant, ephemeral preview).
- Option B: Publish via GitHub Pages (persistent, may require enabling Pages in repo settings).
- Option C: Fix/confirm Vercel deployment and build settings (requires Vercel token or account access). After deploy, also sync to Google Drive per backup policy (Drive OAuth still blocked).

User preference recorded: If asked to change site, prefer the straightforward flow (edit -> commit -> push -> Vercel) and to have Drive backup done automatically.

Source: conversation saved 2026-03-25
